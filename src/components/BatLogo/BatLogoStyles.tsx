import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  title: {
    textAlign: 'center',
    fontWeight: 'bold',
    fontSize: 30,
    color: '#E5BF3C'
  },
  img: {
    resizeMode: 'contain',
    height: 150,
    marginTop: 20
  }
});